on: push

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - shell: make check
  build:
    runs-on: ubuntu-latest
    steps:
      - shell: make build

  test:
    runs-on: ubuntu-latest
    steps:
      - shell: make test